/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config) => {
    //     config.resolve.alias = {
    //         ...config.resolve.alias,
    //         shiki: "shiki/dist/index.browser.mjs",
    //     };
    //     return config;
    // },
};

export default nextConfig;
